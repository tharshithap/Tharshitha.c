#include <stdio.h>

int main() {
  int l,b,perimeter;
  printf("enter the length and breadth :");
  scanf("%d%d",&l,&b);
  perimeter = (2*(l+b));
  printf("the perimeter of triangle %d",perimeter);
  

    return 0;
}
